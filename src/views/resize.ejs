<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head'); %>
  
</head>
<body class="container">

<header>
  <%- include('partials/header'); %>
</header>

<main>
  <div class="jumbotron">
    <h1>Image Resizer</h1>
    <p>Image Processing API</p>
  </div>
  <% if ( images.length > 0 ) { %>
    <div id="image-container">
      <p>In order to resize these images, provide a url as seen below</p>
      <p class='example'>http://localhost:5000/resize?w=500&h=400</p>
      <% images.forEach((img) => { %>
        <img class='sized-image' src="public/rs-images/<%= img %>">
      <% }); %>
    </div>
  <% } else { %>
    <div class='noParams'>
      <h3>Sorry!</h3>
      <p>There is no image to display at this moment</p>
      <p>Provide a url as seen below, you might get lucky that an image was already 
        resized base on the height and width you will provide
      </p>
      <p class='example'>http://localhost:5000/resize?w=500&h=400</p>
    </div>
    
  <% } %>
  
</main>

<footer>
  <%- include('partials/footer'); %>
</footer>

</body>
</html>